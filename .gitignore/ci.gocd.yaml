mypipe:
  materials:
    mygit:
      git: https://github.com/bdpiparva/config_repo_sample/
  stages:
    - build:
        jobs:
          build:
            artifacts:
              - test:
                  source: *.yaml
              - build:
                  source: *.html  
            tasks:
             - exec:
                 command: ls
                 
